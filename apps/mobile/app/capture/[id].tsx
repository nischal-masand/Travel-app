import { EmptyState } from '../../src/components/ui'

// Placeholder — owned by Agent B in the parallel build.
export default function Screen() {
  return <EmptyState title="Capture" body="Not built yet." />
}
