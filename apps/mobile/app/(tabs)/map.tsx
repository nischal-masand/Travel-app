import { EmptyState } from '../../src/components/ui'

// Placeholder — owned by Agent C in the parallel build.
export default function Screen() {
  return <EmptyState title="Map" body="Not built yet." />
}
