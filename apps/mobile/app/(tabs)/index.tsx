import { EmptyState } from '../../src/components/ui'

// Placeholder — owned by Agent A in the parallel build.
export default function Screen() {
  return <EmptyState title="Inbox" body="Not built yet." />
}
